[package]
name = "vibekey"
version = "0.1.0"
edition = "2021"
description = "Third-party driver for the Ulanzi Vibe Key (AU05) — cross-platform core + CLI"

[dependencies]
hidapi = "2"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde_json = "1"
libloading = "0.8"
