# Vibe Key（AU05）第三方客户端：现状、证据与下一步

> 状态快照：2026-08-24。
> 本文记录工程事实与验证边界，不把“已有源码”写成“链路已打通”。

## 结论先行

macOS 的**动作执行端**已经存在：`ProfileStore` 保存四个控件的配置，`ShortcutRunner` 可申请辅助功能权限、激活目标 App、注入组合键，并可通过剪贴板粘贴文本。

设备的**输入软件链路已经接通**：`DeviceMonitor` 能解密 AU05 厂商 HID 报告、读取电量和按键位置，并把按下事件送进 `ProfileStore` / `ShortcutRunner`。但本次验证时设备未枚举，因此这仍是“软件测试通过”，不是“真机快捷键验收通过”。

完整闭环还缺三段：

```text
AU05 控件
  -> Active 查询 + 8 秒心跳       [已实现，待真机确认可进入自定义模式]
  -> 0xFFFC 报告解码为控件事件   [已实现，待真机验收]
  -> ProfileStore 查找映射
  -> ShortcutRunner 执行动作      [已接通，待真机验收]
```

此外，Rust CLI 的状态映射和抓包入口已经存在，但状态动画写回设备仍是明确的 TODO。

---

## 01. 目标

项目目标是为 Ulanzi Vibe Key（AU05）提供独立客户端，优先完成：

1. 六个实体控件可识别、可配置；
2. 面向 Claude Code、Codex、相册整理等场景提供预设；
3. macOS 上可聚焦目标 App、注入快捷键和输入文本；
4. 将 agent 事件映射成设备状态，并最终驱动设备动画；
5. 有线和 2.4 GHz 接收器连接尽量共用同一套 HID 代码。

Rust 代码目前定义了八种设备状态：

| 状态 | 当前事件来源 | 实现状态 |
| --- | --- | --- |
| `idle` | Claude Code / Codex `SessionStart` | 已映射，未写回设备 |
| `thinking` | `UserPromptSubmit` | 已映射，未写回设备 |
| `working` | `PreToolUse` / `PostToolUse`；Claude 另含子代理事件 | 已映射，未写回设备 |
| `error` | Claude `PostToolUseFailure` / `StopFailure` | 已映射，未写回设备 |
| `attention` | `Stop` | 已映射，未写回设备 |
| `notification` | `PermissionRequest`；Claude 另含 `Notification` | 已映射，未写回设备 |
| `sweeping` | Claude `PreCompact` | 已映射，未写回设备 |
| `sleeping` | 暂无事件映射 | 只定义了枚举 |

当前 agent 支持范围只有 `claude-code` 和 `codex`；不存在通用 agent 适配层，也没有必要在协议打通前扩展。

---

## 02. 已确认的设备链路

### 2.1 设备身份与 HID 接口

- 型号：AU05 / Vibe Key
- USB VID：`0xFFF1`
- 当前代码使用的 PID：`0x00DD`

已观察到的 HID 接口：

| Usage Page | Usage | 当前判断 |
| --- | --- | --- |
| `0x0001` | `0x06` | 标准键盘；默认模式的部分面键从这里发键码 |
| `0x000C` | `0x01` | Consumer Control；可能承载音量等媒体控制 |
| `0x0001` | `0x02` | 鼠标接口 |
| `0x0001` | `0x01` | 指针接口 |
| `0xFFFC` | `0x01` | 厂商控制口；自定义模式心跳、按键和控制流所在位置 |

### 2.2 两种工作模式

| 模式 | 表现 | 对第三方客户端的意义 |
| --- | --- | --- |
| 默认模式 | 部分控件走标准 HID | 可做低能力版本，不需要破解厂商协议 |
| 自定义模式 | 官方 App 激活后，控件事件转入 `0xFFFC` 厂商口 | 要实现全控件可编程和状态显示，必须打通这一模式 |

已记录的默认行为：

| 控件 | 默认行为 | 证据强度 |
| --- | --- | --- |
| 麦克风键 | Fn / Globe，可触发听写 | 已做行为观察 |
| 确认键 | Return | 已读到 |
| 取消键 | 默认无标准键码 | 已观察 |
| 侧边键 | 默认无标准键码 | 已观察 |
| 旋钮按下 | 切换输入法，具体键码组合仍需确认 | 行为确认，键码推断 |
| 旋钮转动 | 疑似音量 `Vol-` / `Vol+` | 尚未完成验证 |

因此，“默认模式只靠系统快捷键即可覆盖全部控件”已经被排除。

---

## 03. 已解开的厂商输入报告

### 3.1 已观察到的报告结构

- 厂商输入报告 ID 为 `0x55`，总长 64 字节；
- 约每 10.1 秒出现一次的稳定报告实际是电量通知；
- 已观察到的报告前部：

```text
55 9a e7 f3 05 32 03 f0 db
38 90 c4 99 a3 60 aa ad ...
```

- `38 90 c4 99 a3 60 aa ad` 以 8 字节块重复填充；
- 相同操作的密文具有确定性；
- 输出方向同样是 `0x55` 报告，官方 App 会持续写入多帧动画流，而不是简单的“一种状态一帧”。
- 加密是标准 TEA：32 轮、delta `0x9E3779B9`、8 字节 ECB 分组、小端字序；
- 固定 128 位键已从 `kwdm.dylib` 提取并用抓包帧验证过，但**不随本仓库发布**（见 08 节）；
- 上述捕获帧解密为电量通知：4188mV、100%、充电中、已充满；
- 通知 `0x10` 是按键事件，字段为逻辑索引、状态和物理索引；通知 `0x7B` 是电量事件。

### 3.2 已确认与仍属推断的边界

| 结论 | 状态 |
| --- | --- |
| 报告 ID、长度、重复块、确定性 | 抓包已观察 |
| 收发存在相同的重复密文块 | 抓包已观察 |
| 发送调用链经过 `kwdm.dylib`、`-[HidDevice sendData:value:]`、`IOHIDDeviceSetReport` | lldb 调用栈已观察 |
| 标准 TEA、32 轮、ECB、小端字序 | 静态反汇编与已捕获明密文测试一致 |
| 当前官方库中的固定 128 位键 | 已从 `kwdm.dylib` 提取，并通过已捕获帧验证 |
| 按键和电量通知字段 | 官方消息处理器反汇编与软件测试一致 |
| 键位 `0...3` 对应语音、确认、取消、旋钮 | 官方 AU05 profile 结构支持，待真机逐键验收 |

本地验证阶段曾用自实现的 TEA 解出电量与按键通知；公开仓库不含该实现，Mac App 现在通过厂商 SDK 拿这两类数据。仍未确认的是主动报文能否在官方 App 不运行时独立进入自定义模式，以及旋钮左右转和侧边键的状态值。

### 3.3 已有抓包路径

- DYLD 注入在已签名 App 上被 AMFI 剥离，未成功；
- lldb 断 `IOHIDDeviceSetReport` / `IOHIDDeviceGetReport` 已可观察收发；
- `kwdm.dylib` 的 `_encrypt_data` / `_decrypt_data`、TEA 实现和消息构造器已静态确认；
- 通过只读加载官方库，已取到激活、电量、充电、亮度和心跳请求的 64 字节明文；
- 输出状态动画仍需在设备连接时做最小写入验证。

---

## 04. 蓝牙路径为什么不应优先

现有静态证据强烈指向：AU05 使用 Nordic 2.4 GHz 私有无线协议和配套 USB 接收器，而不是通用 BLE。

证据包括：

1. `libUlanziFZBle.dylib` 的设备名过滤指向 `Ulanzi TC002`，主程序相关发现类也叫 `Tc002Discovery`；
2. AU05 OTA 被拆成 `Dongle(AU05_USB)` 与 `Mic(AU05_Device)` 两段；
3. 主程序含 `NRF~#P`、`DongleSN`、`DongleVersion`、`Paired` 等接收器语义；
4. 已查看的设备来源数据中，AU05 的 `MacAddress` 为空。

这对第三方客户端是利好：有线设备和无线接收器最终都可从 USB HID 层接入，通常不需要另做 BLE 鉴权。

验证边界：实机 BLE 广播扫描因 macOS TCC 权限未完成，所以上述结论是强静态证据，不是 100% 的无线电实测结论。若必须封口，可用手机 nRF Connect / LightBlue 做一次独立扫描。

---

## 05. 仓库当前实际状态

### 5.1 Git 状态

当前 HEAD：`e63c455 style(mac): remove control connector lines`。

| 内容 | Git 状态 | 说明 |
| --- | --- | --- |
| `mac/Sources/VibePal/ProfileStore.swift` | 已提交 | 四个可配置控件、四套内置预设、UserDefaults 持久化 |
| `mac/Sources/VibePal/ShortcutRunner.swift` | 已提交 | 辅助功能权限、激活 App、CGEvent 注入、文本粘贴 |
| `mac/Sources/VibePal/DeviceMonitor.swift` | 已提交 | 枚举 AU05、解密输入、电量与按键事件分发 |
| `mac/Sources/VibePal/HotkeyListener.swift` | 已提交 | 哨兵键（F13–F18）监听，宏模式的输入来源 |
| `mac/Sources/VibePal/Resources/` | 已提交 | App 资源 |
| `mac/Tests/VibePalTests/` | 已提交 | ProfileStore、快捷键显示、相册预设、麦克风归一化测试 |
| `Cargo.toml`、`Cargo.lock`、`src/` | 未跟踪 | Rust CLI 在工作区存在，但不属于当前 Git 提交 |
| `PROTOCOL.md` | 未跟踪 | 协议逆向笔记 |
| `vibe-key-reverse.html` | 未跟踪 | 八节可视化文档 |
| `design/` | 未跟踪 | 设计材料 |
| `mac/.build/`、`mac/build/` | 未跟踪 | 本地构建产物，不应作为源码交付 |

这意味着：换一台机器只克隆当前提交，会得到 Mac App 源码，但不会得到 Rust CLI 和协议笔记，除非之后明确把这些文件纳入版本控制。

### 5.2 Mac App 已实现的能力

- 四个配置对象：旋钮、语音键、确认键、取消键；
- 内置预设：日常工作、Claude、Codex、相册整理；
- 可配置组合键、目标 Bundle ID、可选粘贴文本；
- Claude 预设目标为 `com.anthropic.claudefordesktop`；
- Codex 预设目标为 `com.openai.codex`；
- 相册预设包含 Quick Look 与 Finder 删除组合；
- 通过 Accessibility API 检查或请求辅助功能权限；
- 使用 CGEvent 注入按键，使用系统剪贴板粘贴文本；
- 解密厂商输入报告并读取真实电量；
- 将三个面键和旋钮按下事件接到当前预设的快捷键动作。

尚未实现：

- 侧边键配置；
- 旋钮左转、右转、按下三种动作的独立建模；
- Active 查询和心跳能否独立激活自定义模式的真机验证；
- 状态动画写回设备；
- 物理设备端到端验收。

### 5.3 Rust CLI 已实现的能力

工作区中的 Rust CLI 暴露六个子命令：

| 命令 | 当前状态 |
| --- | --- |
| `list` | 枚举固定 VID/PID 的 AU05 HID 接口 |
| `watch` | 读取指定 Usage Page 的原始输入报告 |
| `map` | 显示 agent 事件对应的设备状态 |
| `hooks` | 生成 hook 配置 JSON |
| `hook` | 读取事件并打印映射结果；写设备仍是 TODO |
| `set-state` | 明确返回失败：输出协议尚未逆向 |

### 5.4 本次验证结果

2026-08-24 本地执行：

- `cargo test`：1 项通过，0 失败；有一个 `Sleeping` 未使用警告；
- `swift test`：36 项通过，0 失败（控件映射、快捷键录制与显示、哨兵键表、动作分发）。

这些测试证明的是纯软件逻辑，不证明：

- 当前机器一定连接了目标硬件；
- 实体按键可触发快捷键；
- 自定义模式能够激活；
- 动画或状态已经写回设备。

---

## 06. 三条实现路线

| 路线 | 做法 | 优点 | 局限 | 结论 |
| --- | --- | --- | --- | --- |
| A. 完整协议 | 在加密前抓明文，确定命令结构，再实现 encode/decode | 能覆盖全部控件和状态显示，最终可独立运行 | 需要动态分析和真机验证 | **已选** |
| B. 密文学习 / 回放 | 记录每个控件与状态的确定性密文 | 可绕过算法识别，较快得到原型 | 可能依赖设备密钥；动画是多帧流；仍需激活握手 | 可作短期验证或兜底 |
| C. 只用标准 HID | 保持默认模式，只映射系统可见控件 | 最简单、风险最低 | 取消键、侧边键等能力缺失，无法控制状态动画 | 只适合缩减版产品 |

路线 A 是合理选择。路线 B 只应在明文断点无法取得时启用；当前没有理由先写复杂的密码分析器。

---

## 07. 下一步执行顺序

### 第 1 步：真机验证输入编号

连接 AU05 后逐项触发语音、确认、取消、旋钮按下/左右转和侧边键，记录解密后的 `index/status/physicalIndex`。

完成标准：每个控件连续触发 20 次无误判，并确认按下、释放、左转、右转的状态值。

### 第 2 步：拆分激活、输入和输出语义

用最小实验矩阵区分：

- 启动时的一次性握手；
- 进入 / 退出自定义模式；
- 六个控件的输入事件；
- 心跳 / 保活；
- 八种状态的动画帧序列。

完成标准：断开官方 App 后，能说明设备处于什么模式、为什么会继续或停止上报控件事件。

### 第 3 步：补 Rust codec

输出方向（状态动画写回设备）仍是 TODO；当前经由厂商 SDK 的 `setDeviceLedEffect` 做标定。

完成标准：Rust 程序独占设备时，能够识别同一实体控件至少 20 次且无误判。

接收器 PID 是否不同也应在此时通过真机枚举确认，再决定是否放宽 PID；不能仅凭推测把所有 `0xFFF1` 设备都当成 AU05。

### 第 4 步：完善 Mac App 控件模型

输入主链已接通；确认真机状态值后，补上侧边键与旋钮左转、右转、按下三种独立配置。

完成标准：四套预设可由真机触发；权限未授予、目标 App 不存在时有可理解的 UI 状态。

### 第 5 步：做物理验收

至少覆盖：

- 有线与无线接收器各一次；
- 六个控件逐项触发；
- Claude、Codex、相册整理预设；
- 断连、重连、睡眠唤醒；
- 官方 App 未运行时独立工作；
- 状态动画循环、停止和错误恢复。

完成标准：区分“构建通过”“软件测试通过”“真机输入通过”“真机输出通过”四个门槛，不能互相代替。

---

## 08. 工程与法律边界

本节是工程范围约束，不构成针对具体司法辖区的法律意见。

美国《版权法》17 U.S.C. §1201(f) 对为了让独立程序实现互操作而进行的必要识别、分析和相应手段设有条件性例外；欧盟《计算机程序法律保护指令》2009/24/EC 第 6 条也在满足必要性、合法使用资格、信息此前不可得、范围限于必要部分等条件时规定互操作性反编译例外。它们都不是“所有逆向行为自动合法”的通行证。

项目应坚持：

1. 只分析自己有权使用的硬件和软件副本；
2. 目的限于独立客户端与设备互操作；
3. 不重新分发官方 App、固件、密钥、图标或其他版权素材；
4. 不绕过付费、订阅、账号授权或访问控制来获取付费能力；
5. 只记录实现互操作所需的协议事实，并保留观察日期、工具和来源；
6. 不向设备发送未经最小验证的破坏性报告，固件升级和恢复路径保持可用；
7. 对外发布前，按实际分发地区和最终实现方式做一次针对性的法律审查。

参考原文：

- [17 U.S.C. §1201(f)](https://www.law.cornell.edu/uscode/text/17/1201)
- [Directive 2009/24/EC, Article 6](https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=CELEX:32009L0024)

---

## 证据来源

- `mac/Sources/VibePal/ProfileStore.swift`
- `mac/Sources/VibePal/ShortcutRunner.swift`
- `mac/Sources/VibePal/DeviceMonitor.swift`
- `mac/Tests/VibePalTests/ProfileStoreTests.swift`
- `src/main.rs`
- `src/device.rs`
- `src/mapping.rs`
- `PROTOCOL.md`
- `vibe-key-reverse.html`
- Ulanzi Studio.app 静态字符串、HID 报告与 lldb 动态观察（记录于 `PROTOCOL.md`）

这份文档是当前仓库状态基线。后续每次协议突破，只更新“已确认”与对应的验证证据；没有真机结果时，继续标记为推断或未验证。
